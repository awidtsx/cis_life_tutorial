<%= form_with(model: agreement_contract) do |form| %>
  <% if agreement_contract.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(agreement_contract.errors.count, "error") %> prohibited this agreement_contract from being saved:</h2>

      <ul>
        <% agreement_contract.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :insurance_product_id, style: "display: block" %>
    <%= form.text_field :insurance_product_id %>
  </div>

  <div>
    <%= form.label :cooperative_id, style: "display: block" %>
    <%= form.text_field :cooperative_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
