<%= form_with(model: add_province) do |form| %>
  <% if add_province.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(add_province.errors.count, "error") %> prohibited this add_province from being saved:</h2>

      <ul>
        <% add_province.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :add_region_id, style: "display: block" %>
    <%= form.text_field :add_region_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
