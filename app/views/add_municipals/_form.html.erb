<%= form_with(model: add_municipal) do |form| %>
  <% if add_municipal.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(add_municipal.errors.count, "error") %> prohibited this add_municipal from being saved:</h2>

      <ul>
        <% add_municipal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :add_province_id, style: "display: block" %>
    <%= form.text_field :add_province_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
