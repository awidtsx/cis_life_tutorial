<div class="col-md-4">
  <div class="card h-100 shadow-sm">
    <div class="card-header text-white fw-bold" style="background-color:<%= color %>;">
      <%= title %>
    </div>

    <ul class="list-group list-group-flush">
      <% items.each do |item| %>
        <li class="list-group-item d-flex justify-content-between align-items-center">
        <%= link_to item[:path],
class: "d-flex justify-content-between align-items-center text-decoration-none text-dark module-link" do %>
          <span><%= item[:label] %></span>
          <span class="text-muted">→</span>
          <% end %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
