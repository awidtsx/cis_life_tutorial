<%= form_with(model: add_barangay) do |form| %>
  <% if add_barangay.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(add_barangay.errors.count, "error") %> prohibited this add_barangay from being saved:</h2>

      <ul>
        <% add_barangay.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :add_municipal_id, style: "display: block" %>
    <%= form.text_field :add_municipal_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
