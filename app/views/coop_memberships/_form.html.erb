<%= form_with(model: coop_membership) do |form| %>
  <% if coop_membership.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(coop_membership.errors.count, "error") %> prohibited this coop_membership from being saved:</h2>

      <ul>
        <% coop_membership.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :cooperative_id, style: "display: block" %>
    <%= form.text_field :cooperative_id %>
  </div>

  <div>
    <%= form.label :individual_id, style: "display: block" %>
    <%= form.text_field :individual_id %>
  </div>

  <div>
    <%= form.label :membership_date, style: "display: block" %>
    <%= form.date_field :membership_date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
