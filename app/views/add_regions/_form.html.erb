<%= form_with(model: add_region) do |form| %>
  <% if add_region.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(add_region.errors.count, "error") %> prohibited this add_region from being saved:</h2>

      <ul>
        <% add_region.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
